import { CreditdebitDirective } from './creditdebit.directive';

describe('CreditdebitDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditdebitDirective();
    expect(directive).toBeTruthy();
  });
});
